# Android App Module

Kotlin + Jetpack Compose Material You UI.

Will host:
- Cube visualizer (3D or 2D net)
- Scramble input / camera color recognition (future)
- Solver controls (choose size n, method, speed)
- APK production builds via GitHub Actions / local signed builds

Open the whole project in Android Studio once the Gradle skeleton lands.
